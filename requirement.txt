# Core LiveKit / AI assistant runtime
# Versions pinned to a known-good, mutually-compatible set (checked against
# PyPI on 2026-09-12). Unpinned deps were the reason the assistant could
# silently pick up a breaking release on a fresh install — bump these
# deliberately and re-test, don't leave them floating.
livekit==1.1.18
livekit-agents==1.8.1
livekit-plugins-google==1.8.1
livekit-plugins-noise-cancellation==0.3.0
livekit-plugins-openai==1.8.1
livekit-plugins-silero==1.8.1
google-genai==2.23.0
google-generativeai==0.8.6
mem0ai
langchain
langchain-community

# Environment, search, HTTP, and translation utilities
python-dotenv
requests
duckduckgo-search
deep-translator

# Legacy/optional client library (unused by current tools, kept as-is)
websocket-client

# Windows desktop automation, audio, and system control
pywin32
comtypes
pycaw
screen-brightness-control
PyAutoGUI
PyGetWindow
pyperclip
psutil
sounddevice

# Vision (screen capture / analysis)
pillow

# Fuzzy matching
fuzzywuzzy
python-Levenshtein
