version: '3.8'

services:
  brain-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: jarvis-brain-server
    ports:
      - "8000:8000"
    restart: unless-stopped
    environment:
      - PORT=8000
    volumes:
      - ./uploads:/app/uploads
